# --- Núcleo de edición de fotos ---
numpy>=1.24
Pillow>=10.0
rawpy>=0.18

# --- Interfaz visual (app web local) ---
Flask>=3.0

# --- Vídeo ---
# ffmpeg: usa el del sistema si lo tienes; si no, este paquete trae uno listo.
imageio-ffmpeg>=0.4.9

# --- Opcional: detección de caras para elegir la mejor foto/clip ---
# Si no se instala, el resto de criterios (enfoque, exposición...) siguen
# funcionando.
opencv-python-headless>=4.8

# --- Opcional: leer fotos HEIC/HEIF (p. ej. de iPhone) ---
pillow-heif>=0.13
